# Telegram credentials are configured only on the production server in
# public/api/lead-config.php (ignored by Git) or via server environment variables.
# Never add bot tokens or chat IDs to VITE_* variables.
